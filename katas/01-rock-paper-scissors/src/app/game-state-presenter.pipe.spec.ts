import { GameStatePresenterPipe } from './game-state-presenter.pipe';

describe('GameStatePresenterPipe', () => {
  it('create an instance', () => {
    const pipe = new GameStatePresenterPipe();
    expect(pipe).toBeTruthy();
  });
});
